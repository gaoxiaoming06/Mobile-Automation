# Source Scanner

Deterministic source-code scanner for building draft Business Graph candidates.

The scanner is intentionally read-only. It does not write automation contracts into business repositories and it does not publish active graph versions directly. Results are draft candidates that should be reviewed, merged, or rejected by the platform.
